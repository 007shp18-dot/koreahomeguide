import {describe,it,expect,vi} from 'vitest';
vi.mock('server-only',()=>({}));
vi.mock('../lib/public-market/korea-evidence-repositories.server',()=>({koreaEvidenceRepositoriesFromEnvironment:()=>({sale:{listBuildingRecords:()=>[...Array.from({length:25},(_,i)=>({buildingId:`other-${i}`,officialName:`가나다${i}`,districtSlug:'gangseo-gu',neighborhoodName:'등촌동',housingType:'apartment',recentSales:[{areaSqm:84}]})),{buildingId:'target',officialName:'등촌1,2단지주공아파트',districtSlug:'gangseo-gu',neighborhoodName:'등촌동',housingType:'apartment',recentSales:[{areaSqm:41.85}]}]},rent:null})}));
import {GET} from '../app/api/seoul/building-search/route';
describe('Check building autocomplete',()=>{it('ranks name matches before neighbourhood matches before limiting results',async()=>{const response=await GET(new Request('https://signedprice.com/api/seoul/building-search/?q='+encodeURIComponent('등촌')));const body=await response.json();expect(body.items).toHaveLength(20);expect(body.items[0]).toMatchObject({id:'target',name:'등촌1,2단지주공아파트',area:41.85})});it('returns no entries for short input',async()=>{expect(await(await GET(new Request('https://signedprice.com/api/seoul/building-search/?q=a'))).json()).toEqual({items:[]})})});
