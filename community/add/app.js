(function() {
  'use strict';
  const form = document.querySelector('#communityContribution');
  if (!form || document.body.dataset.communityEnabled !== 'true') return;
  // Network persistence is intentionally absent until durable storage and moderation are configured.
})();
